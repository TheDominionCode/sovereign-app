import { ComingSoon } from "../_components/coming-soon";

export default function SpeakPage() {
  return (
    <ComingSoon
      title="Speak Eloquently"
      intent="A language refinement library — built-in phrases (saying no, disagreeing, hard conversations) plus your own custom additions."
      tables={["elegant_phrases"]}
    />
  );
}
